import '/config/all_imports.dart';

class HomeModel {
  bool status;
  String message;
  DataHomeModel data;

  HomeModel({
    required this.status,
    required this.message,
    required this.data,
  });
}
